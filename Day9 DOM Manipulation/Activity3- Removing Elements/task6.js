(function main() {
    // select the target HTML element by its id
    const requiredNode = document.getElementById('task6');

    // remove last child element.
    requiredNode.lastElementChild.remove();
})();