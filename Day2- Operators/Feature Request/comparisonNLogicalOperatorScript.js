let numOne = 7;
let numTwo = 13;

if(numOne > numTwo && numOne===numTwo)
    console.log(`${numOne}is greater than or equal to ${numTwo} `);
else
    console.log(`${numOne} is lesser than or equal to ${numTwo}`);