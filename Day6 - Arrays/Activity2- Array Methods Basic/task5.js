function main() {
    let arr = [1, 2, 3, 4, 5, 6];
    // Original array
    console.log(`Original array: ${arr}`);

    // shift method is used to remove first item from the array.
    arr.shift();

    // Updated array
    console.log(`Updated array: ${arr}`);
}

main()